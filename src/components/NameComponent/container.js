/* Component in charge of managing logic, obtaining and sending information */
import NameComponentView from './presentational';

const NameComponent = () => (
  <NameComponentView />
);

export default NameComponent;
